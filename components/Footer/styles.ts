import styled from "styled-components";

export const FooterComponent = styled.footer`
  position: relative;
  bottom: 0;
  left: 0;
  right: 0;
  color: #fafaf9;
  height: 80px;
  background: rgb(239, 137, 100);
  background: linear-gradient(
    90deg,
    rgba(239, 137, 100, 1) 0%,
    rgba(179, 64, 46, 1) 100%
  );

  display: flex;
  align-items: center;
  justify-content: center;
`;
